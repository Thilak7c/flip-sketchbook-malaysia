// Tuning constants for the "curved leaf" page turn.
export const STRIP_COUNT = 18; // strips per turning leaf — enough for a smooth curve
export const SPAN = 0.449; // gutter → outer page edge, as a fraction of book width
export const BETA = 0.6; // peak curl of the arc, radians