# Asset status

The archive includes the optimized `spread-george-town.webp` and the transparent hibiscus mark when available in the local asset bundle.

The current page data also references these future files:

- `spread-kilim-langkawi.png`
- `spread-kinabalu-sabah.png`
- `spread-melaka.png`

Add the corresponding generated watercolor spreads to `public/assets/` using those filenames, or update the `landmarks` array in `src/app/page.tsx` to match your own assets. Keep all spreads at the same landscape aspect ratio for consistent page-turn behavior.
